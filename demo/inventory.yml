---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
    rhel01.nostromo.io:
      ansible_user: root
    rhel03.nostromo.io:
      ansible_user: root

